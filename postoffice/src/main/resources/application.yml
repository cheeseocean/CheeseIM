spring:
  data:
    redis:
      host: ${store.host.redis:localhost}
      port: 6379
      password: ${redis.password:}
      database: 1
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5