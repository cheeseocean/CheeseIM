# 全局生成约束

1. 目标项目为 **Spring Boot 3 + Dubbo 3 + Gradle 多模块 + MongoDB**。
2. 所有常量优先使用枚举，禁止魔法值。
3. Dubbo 服务接口、实现类、注入对象命名禁止带 `Dubbo` 或 `Rpc`。
4. 所有公共类、接口、枚举、方法、重要字段注释必须使用中文。
5. 代码优先保证可读性、可维护性、可扩展性。
6. 使用构造器注入，禁止字段注入。
7. 持久化对象与领域对象必须分离。
8. `domain` 层不得依赖 MongoDB 具体实现。
