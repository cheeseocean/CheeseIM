# 参数校验与日志规范

## 参数校验
- 优先使用 `jakarta.validation`
- 必要时补充显式防御式校验
- 禁止完全依赖前端校验

## 日志规范
- 使用参数化日志
- 记录关键业务节点
- 记录错误上下文
- 禁止字符串拼接日志
- 禁止打印敏感信息

## 正确示例

```java
log.info("创建用户成功, userId={}", userId);
log.error("查询用户失败, userId={}", userId, e);
```
